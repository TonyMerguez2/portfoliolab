fastapi==0.110.0
uvicorn[standard]==0.27.1
pydantic==2.6.1
pydantic-settings==2.2.1

# Base de données, comptes et téléversements
#
# ⚠️ Ces cinq lignes manquaient, et rien ne le disait tant qu'on ne travaillait
# que sur cette machine : elles étaient installées dans le venv local depuis les
# chantiers qui les ont introduites — la base, puis les comptes, puis les images
# de portefeuille — sans jamais être déclarées. Le défaut n'est apparu qu'à la
# première installation ailleurs : l'API montait, puis mourait sur
# `ModuleNotFoundError: No module named 'sqlalchemy'`.
#
# Un fichier de dépendances ne se vérifie que sur une machine vierge. Celle-ci
# l'a fait pour nous.
SQLAlchemy==2.0.50
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32
requests==2.34.2

# Financial data
#
# ⚠️ **1.4.1 et non 0.2.36 : la version épinglée ne fonctionne plus du tout.** Yahoo a changé
# ses points d'entrée depuis février 2024, et 0.2.36 reçoit du HTML là où elle attend du JSON —
# `JSONDecodeError: Expecting value: line 1 column 1`. Mesuré côte à côte : sur le serveur, en
# 0.2.36, AAPL rendait **zéro** ligne d'historique et aucune fiche ; sur la machine de travail,
# en 1.4.1, cinq lignes et 187 champs.
#
# Le venv local avait été mis à jour sans que le fichier suive, comme pour SQLAlchemy plus
# haut : le site marchait ici et affichait des courbes vides ailleurs. Deuxième dérive du même
# genre, et le même remède — un fichier de dépendances ne se vérifie que sur une machine neuve.
yfinance==1.4.1
pandas==2.2.0
numpy==1.26.4
scipy==1.12.0

# HTTP
httpx==0.27.0

# Dev
pytest==8.0.1
pytest-asyncio==0.23.5
black==24.2.0
ruff==0.2.2
