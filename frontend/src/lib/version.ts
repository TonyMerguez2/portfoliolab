/**
 * L'étiquette de version affichée à côté de la marque.
 *
 * ⚠️ **Ce n'est pas la version du paquet npm.** `package.json` annonce 1.0.0 depuis le
 * premier commit et ne veut rien dire ; ce que cette pastille annonce au visiteur, c'est
 * l'état du produit — une alpha fermée. Les deux nombres n'ont pas à se rejoindre : l'un
 * sert à l'outillage, l'autre est une promesse faite au lecteur.
 *
 * À changer à la main quand l'état change (bêta, puis 1.0).
 */
export const VERSION = "Alpha 0.1";
